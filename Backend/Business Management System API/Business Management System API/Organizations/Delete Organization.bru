meta {
  name: Delete Organization
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/organizations/{{organizationId}}
  body: none
  auth: inherit
}

settings {
  encodeUrl: true
}

docs {
  Delete organization (Admin only)
}
