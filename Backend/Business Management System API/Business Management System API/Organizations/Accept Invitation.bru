meta {
  name: Accept Invitation
  type: http
  seq: 8
}

post {
  url: {{baseUrl}}/organizations/accept-invitation
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "token": "invitation-token-here",
    "password": "newpassword123",
    "name": "New User Name"
  }
}

settings {
  encodeUrl: true
}

docs {
  Accept organization invitation and set password
}
