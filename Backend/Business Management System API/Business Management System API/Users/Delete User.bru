meta {
  name: Delete User
  type: http
  seq: 4
}

delete {
  url: {{baseUrl}}/users/:id
  body: none
  auth: inherit
}

params:path {
  id: user-id
}

settings {
  encodeUrl: true
}

docs {
  Delete user from organization
}
