meta {
  name: Get All Supplier Orders
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/organizations/{{organizationId}}/orders?status=PENDING
  body: none
  auth: inherit
}

params:query {
  ~status: PENDING
}

headers {
  Authorization: Bearer {{token}}
}

settings {
  encodeUrl: true
}
