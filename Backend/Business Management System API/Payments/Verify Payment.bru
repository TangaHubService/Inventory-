meta {
  name: Verify Payment
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/payments/verify
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "transactionToken": "dpo-transaction-token"
  }
}

settings {
  encodeUrl: true
}

docs {
  Verify payment status with DPO Pay
}
