meta {
  name: Business Management System API
}

auth {
  mode: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:5000/api
  token: 
  organizationId: 
}

docs {
  Complete API collection for Multi-Domain Business Inventory & Sales Management System (Pharmacy, Hardware Store, Retail, etc.)
}
