meta {
  name: Change Password
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/change-password
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "currentPassword": "oldpassword123",
    "newPassword": "newpassword123"
  }
}

settings {
  encodeUrl: true
}

docs {
  Change user password (required for invited users)
}
