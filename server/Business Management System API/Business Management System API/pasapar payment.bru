meta {
  name: pasapar payment
  type: http
  seq: 15
}

post {
  url: {{baseUrl}}/subscriptions/organizations/{{organizationId}}/plans/{{planId}}/pesapal/initiate
  body: json
  auth: inherit
}

settings {
  encodeUrl: true
  timeout: 0
}
